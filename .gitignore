# deps / build
node_modules/
dist/
.cache/
# personal / secrets
CLAUDE.local.md
.claude/settings.local.json
.env
# full scraped corpus (curated subset only is committed)
research/corpus-full/
*.zip
# test artifacts (headless render screenshots, etc.)
test/artifacts/
workshop/artifacts/
# os
.DS_Store

# SDD working scratch (briefs, reports, review diffs, ledger)
.superpowers/

# throwaway spikes (Astro embed proof, etc.)
spike/

# web-ready splat assets — large binaries delivered via host/CDN, never committed
# (repo-lean / inode-cap rule). See immersive/public/assets/README.md.
immersive/public/assets/*.spz
immersive/public/assets/*.sog
immersive/public/assets/*.ply
immersive/public/assets/*.ksplat

# ambient playlist audio — CC0/CC-BY tracks delivered via host/CDN, never committed
# (same repo-lean rule as splats). See immersive/public/assets/audio/README.md.
immersive/public/assets/audio/*.ogg
immersive/public/assets/audio/*.mp3
immersive/public/assets/audio/*.m4a
immersive/public/assets/audio/*.opus
immersive/public/assets/audio/*.wav
immersive/public/assets/audio/*.flac
