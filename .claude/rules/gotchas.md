# Gotchas / Time-wasters — read before re-debugging these

Distilled tribal knowledge so the same loops don't recur (anti-footgun manual,
trailofbits pattern). Add to this when a non-obvious failure costs real time.

- **`npm run health` shows a render.png drift FAIL in a fresh container — this is
  EXPECTED.** That PNG (under test/artifacts/, gitignored) is generated by
  `test/render-check.mjs` (needs Playwright Chromium), so it's absent at rest. Not a
  regression. (Path written un-backticked here on purpose so this very rule doesn't
  add another drift-gate hit for the missing file.)
- **Looks must be fetched relative to the module, not the page.** `src/looks/registry.js`
  resolves JSON via `import.meta.url` (with an inline `INLINE_LOOKS` `file://` fallback);
  fetching `/looks/*.json` 404s. Don't "fix" it back to a page-relative path.
- **The render screenshot must freeze the loop first.** On CI software-GL the rAF loop
  starves the CPU; `test/render-check.mjs` pauses via `window.__primordial.pause` before
  capturing. Don't remove the freeze.
- **The cloud container is HTTPS-443 only.** FTP/21 and cPanel/2083 are blocked — you
  cannot deploy or drive cPanel from here. Push → GitHub Actions FTPS deploys; verify by
  `curl`-ing `https://primordial.video/Test/`.
- **Only git-committed files survive a session.** Auto-memory and `~/.claude/plans` do
  not persist. Commit durable state (`progress.md`/`task_plan.md`) before the session ends.
- **Don't defer with "I'll handle X later."** A TODO dropped mid-reply silently
  vanishes — nobody re-reads it. Either do it now, or park it explicitly (the `/park`
  skill → `progress.md` "Open threads", which the orient hook resurfaces). "Later"
  without a parked thread = lost.

## Behavioral pitfalls live in the always-loaded rules (don't duplicate here)

The recurring *agent-behaviour* mistakes are already captured where they load every
session — keep them there, not copied into this file:

- **Wrong-referent** ("show *me*" vs the live deliverable) and **unverified
  assumptions** → the **Accuracy rule** (CLAUDE.md, always-loaded).
- **Over-long / jargon-heavy replies** → the **Communication rule** (CLAUDE.md).
- **Handing steps to a phone operator** → `.claude/rules/mobile-ergonomics.md`.
